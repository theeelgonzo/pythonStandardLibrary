# Zipfile Module
import zipfile
# Open and List
zip = zipfile.ZipFile('Archive.zip', 'r')
print(zip.namelist())
# Metadata in the zip folder

# Access to files in zip folder

# Extracting files

# Closing the zip

